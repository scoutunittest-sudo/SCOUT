Summarize this Java class as class-wide context for method scenario analysis.

Compact output rules:
- At most 4 bullets.
- Focus on class-wide role, mutable state, invariants, collaborators, and observable contract.
- Keep only facts that apply across methods or constrain many methods.
- Do not summarize each method.
- Do not explain Java syntax.
- Omit private implementation steps unless they define a class invariant.

Project summary:
${project_summary!""}

Class name:
${class_name!""}

Class code:
${class_code!""}

Return the summary inside [SUMMARY] and [/SUMMARY].
